from django.apps import AppConfig


class ProtocolosConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "protocolos"
    verbose_name = "Central de Protocolos (eProtocolo)"
