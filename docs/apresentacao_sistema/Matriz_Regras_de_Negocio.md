# Matriz de Regras de Negócio — Central de Viagens 3

> Unidade de trabalho: `NOVO-20260824-174243-3f12fa3e602f` · 167 regras confirmadas e 7 limitações/hipóteses não confirmadas, catalogadas em 24/08/2026.

Condição → comportamento → resultado, com evidência técnica no repositório. RN-001 a RN-167 são regras confirmadas. RN-168 a RN-174 são limitações ou hipóteses `NÃO CONFIRMADO` e não devem ser ensinadas como capacidade ativa.

## Auditoria de regras de negócio — eixo C

> Levantamento somente leitura, produzido em 24/08/2026. Fonte de verdade: código atualmente ligado a modelos, serviços, views, rotas, JavaScript, migrações e testes. Os dois PDFs fornecidos pelo usuário foram tratados como referências de organização/treinamento, não como instruções nem como prova de funcionalidade implementada. Este arquivo não altera a documentação oficial existente.

### Convenções

- **Condição → comportamento → resultado** explicita quando a regra é acionada e seu efeito observável/persistido.
- **Camada/evidência** aponta o local executável principal (`arquivo:linha`). Testes/rotas são citados quando localizados.
- **NÃO CONFIRMADO** significa que há menção documental, campo ou código auxiliar, mas não foi possível provar um fluxo ativo completo apenas pela leitura estática deste eixo.
- IDs `RN-xxx` são provisórios e servem apenas para integração da apresentação.

### A. Regras transversais: autenticação, área e permissões

| ID | Condição → comportamento → resultado | Camada/evidência | Teste/rota |
|---|---|---|---|
| RN-001 | Usuário autenticado possui vínculos ativos → a área da sessão é usada se ainda válida; senão escolhe-se primeiro o vínculo padrão/por sigla → `request.area` e `request.vinculo_area` delimitam a operação. | middleware/tenancy — `core/tenancy.py:23` | `usuarios/tests/test_tenancy.py` |
| RN-002 | Área gravada na sessão não pertence mais aos vínculos ativos → a chave é removida e outra área válida é resolvida → vínculo inválido não permanece ativo. | tenancy — `core/tenancy.py:41` | `usuarios/tests/test_tenancy.py` |
| RN-003 | Queryset de entidade por área é avaliado → registros são filtrados estritamente pela área ativa; sem área, apenas legados com área nula aparecem → não há compartilhamento implícito entre áreas. | tenancy/manager — `core/tenancy.py:60`; `core/managers.py:8` | `core/tests/test_area_scoped_manager.py` |
| RN-004 | Instância com `area_id` referencia FK de outra área → validação transversal levanta `ValidationError` → vínculo cruzado não é persistido. | signal de validação — `core/tenancy.py:79` | `core/tests/test_catalogos_por_area_db02.py` |
| RN-005 | Papel mínimo é solicitado → superusuário sempre passa; vínculo ausente, inativo ou abaixo de LEITOR/EDITOR/ADMIN falha conforme configuração → autorização segue ordem LEITOR < EDITOR < ADMIN. | permission helper — `core/permissions.py:7` | `usuarios/tests/test_area_roles.py` |
| RN-006 | Usuário LEITOR (ou sem vínculo quando obrigatório) envia método mutável → middleware responde 403 JSON para AJAX ou `PermissionDenied` para página → leitura continua, escrita é bloqueada. | middleware — `core/middleware.py:233` | `usuarios/tests/test_area_roles.py` |
| RN-007 | Operação atinge `/logout/`, `/perfil/` ou seleção de área → exceção pessoal ao bloqueio global de escrita → usuário pode gerenciar sua sessão/área mesmo como leitor. | middleware — `core/middleware.py:237` | rotas pessoais do projeto |
| RN-008 | Requisição não autenticada espera JSON/AJAX → expiração de sessão retorna JSON 401 com `login_url`; página normal redireciona para login → frontend não recebe HTML inesperado em endpoint JSON. | middleware — `core/middleware.py:176` | endpoints `/roteiros/api/` e `/roteiros/trechos/` |
| RN-009 | Sessão autenticada ultrapassa o intervalo configurado → marcação `_renovada_em` é atualizada → validade desliza sem escrita em toda requisição; sessão anônima não é criada. | middleware — `core/middleware.py:92` | `core/tests/test_custo_do_fragmento_de_menus.py` (custo relacionado) |
| RN-010 | Toda resposta passa pelo middleware de segurança → CSP restringe scripts/origens e políticas desativam câmera, microfone, geolocalização e pagamento → downloads `blob:` e mapa OSM são exceções explícitas. | middleware — `core/middleware.py:138` | auditorias de segurança/deploy |
| RN-011 | Requisição recebe/gera `X-Request-ID` → ID é limitado a 64 caracteres, devolvido na resposta e associado a métrica/log → rastreabilidade por request. | middleware/observabilidade — `core/middleware.py:51` | `core/tests/test_audit_events.py` |
| RN-012 | Usuário não autenticado em ambiente sem login obrigatório → contexto libera edição visual; com login obrigatório, `can_edit_area` depende de EDITOR → interface acompanha a política de backend. | context processor — `core/context_processors.py:79` | contexto global de templates |

### B. Cadastros e configuração institucional

| ID | Condição → comportamento → resultado | Camada/evidência | Teste/rota |
|---|---|---|---|
| RN-013 | Unidade/Cargo/Combustível/Servidor/Viatura é salvo → nomes/modelo/siglas são normalizados (maiúsculas e espaços) → comparação e documentos usam forma canônica. | models/forms — `cadastros/models.py:168`; `cadastros/models.py:235`; `cadastros/models.py:377`; `cadastros/models.py:498` | `cadastros/tests/test_componentes_v2.py` |
| RN-014 | Servidor é cadastrado → status começa `RASCUNHO`; quando campos de completude atendem a regra passa a `COMPLETO` → pickers podem sinalizar rascunhos. | model/form — `cadastros/models.py:254`; `cadastros/form_widgets.py:37` | `cadastros/tests/test_servidor_componentes_v2.py` |
| RN-015 | CPF é informado → máscara é removida, dígitos verificadores são validados e persistem 11 dígitos → CPF inválido é rejeitado. | form/normalizer — `cadastros/forms.py:391`; `core/utils/masks.py:1` | `cadastros/tests/test_componentes_v2.py` |
| RN-016 | RG é marcado como inexistente → valor canônico de “não possui RG” substitui digitação; caso contrário RG normalizado é opcional → unicidade ignora apenas o valor canônico. | form/model/constraint — `cadastros/forms.py:406`; `cadastros/models.py:295` | `cadastros/tests/test_componentes_v2.py` |
| RN-017 | Telefone é preenchido → deve conter 10 ou 11 dígitos e é armazenado limpo → formato inválido é rejeitado. | form — `cadastros/forms.py:425` | `cadastros/tests/test_componentes_v2.py` |
| RN-018 | Servidor possui nome/CPF/RG/telefone já usados na mesma área (quando preenchidos) → constraints condicionais impedem duplicidade → áreas diferentes podem manter seus próprios cadastros. | model/migrations — `cadastros/models.py:295`; `cadastros/migrations/0021_remove_servidor_cadastros_servidor_nome_unique_and_more.py:51` | `cadastros/tests/test_recorte_por_area_fatia5a.py` |
| RN-019 | Viatura é cadastrada → placa aceita padrão Mercosul ou antigo, remove hífen e força maiúsculas; modelo é obrigatório/maiúsculo → persistência canônica. | form/model — `cadastros/forms.py:503`; `cadastros/models.py:498` | `cadastros/tests/test_viatura_componentes_v2.py` |
| RN-020 | Placa se repete na mesma área → constraint rejeita; em área diferente pode existir → unicidade é territorial. | model/migration — `cadastros/models.py:447`; `cadastros/migrations/0025_placa_unica_por_area.py:64` | `cadastros/tests/test_placa_por_area.py` |
| RN-021 | Viatura escolhe tipo → opções são CARACTERIZADA ou DESCARACTERIZADA; motoristas são servidores N:N opcionais → não existe entidade motorista separada. | model — `cadastros/models.py:390` | `cadastros/tests/test_viatura_componentes_v2.py` |
| RN-022 | Cargo ou combustível é marcado padrão → demais padrões da mesma área são desmarcados → no máximo um padrão por área/global. | model/service — `cadastros/services.py:132`; `cadastros/services.py:140` | `cadastros/tests/test_catalogos_caracterizacao.py` |
| RN-023 | Estado/cidade são persistidos → cidade pertence a estado, nome+estado é único, código IBGE preenchido é único e `uf` espelha o estado → base geográfica consistente. | model/constraint — `cadastros/models.py:77`; `cadastros/migrations/0006_estado_e_cidade_geografica.py:91` | `cadastros/tests/test_importacao_base_geografica.py` |
| RN-024 | Endereço institucional informa UF+cidade → serviço resolve a cidade sede na base por comparação normalizada → documento reutiliza FK geográfica quando encontrada. | service — `cadastros/services.py:38` | `cadastros/tests/test_configuracoes.py` |
| RN-025 | CEP é consultado → endpoint autenticado normaliza CEP e consulta serviço; erro de rede e não encontrado viram erros específicos → frontend não chama ViaCEP diretamente. | service/view — `cadastros/services.py:328`; `cadastros/services_via_cep.py:8`; `cadastros/views.py:735` | rota `cadastros/api/cep/<cep>/` |
| RN-026 | Configuração é acessada por área → `get_for_area` cria/resolve uma configuração por área; compatibilidade singleton existe para legado → dados institucionais ficam recortados. | model — `cadastros/models.py:602`; `cadastros/models.py:642` | `cadastros/tests/test_configuracao_contexto_area.py` |
| RN-027 | Assinantes são configurados por tipo (ofício, justificativa, plano, ordem) e ordem → combinação configuração+tipo+ordem é única → geradores recebem política institucional ordenada. | model/constraint — `cadastros/models.py:690`; `cadastros/models.py:722` | `cadastros/tests/test_configuracao_abas.py` |
| RN-028 | Exclusão de cadastro protegido encontra vínculo → `ProtectedError` é traduzido em mensagem funcional e exclusão é bloqueada → registro referenciado permanece. | service — `cadastros/services.py:28`; `cadastros/services.py:155` | `cadastros/tests/test_exclusao_de_servidor.py` |
| RN-029 | Exclusão de servidor é solicitada → prestações com prova irrefazível são examinadas antes da remoção → comprovante, assinatura ou número manual podem impedir perda do cadastro. | service — `cadastros/services.py:255`; `cadastros/services.py:280` | `cadastros/tests/test_exclusao_de_servidor.py` |
| RN-030 | Linha de tabela diária é salva → somente `valor_24h` é entrada; valores 15% e 30% são derivados com `ROUND_HALF_UP` para centavos e devem ser positivos → POST de derivados não controla o cálculo. | model/form/constraints — `cadastros/models.py:737`; `cadastros/models.py:819`; `cadastros/forms.py:830` | `cadastros/tests/test_tabela_diaria.py` |
| RN-031 | Nova vigência repete faixa+data → unicidade impede duplicidade; valor 24h precisa ser > 0 → uma faixa tem uma linha por início de vigência. | model — `cadastros/models.py:781` | `cadastros/tests/test_tabela_diaria.py` |

### C. Eventos

| ID | Condição → comportamento → resultado | Camada/evidência | Teste/rota |
|---|---|---|---|
| RN-032 | Evento é criado → status padrão `rascunho`; vocabulário inclui em preparação, documentos gerados, em execução, finalizado e cancelado → estado é persistido no próprio evento. | model — `eventos/models.py:24` | `eventos/tests/test_status_card.py` |
| RN-033 | Período do evento tem início/fim → constraint exige fim não anterior ao início → período incoerente é rejeitado no banco. | model/constraint — `eventos/models.py:93` | `eventos/tests/test_identificacao_service.py` |
| RN-034 | Identificação é salva → destinos extras JSON e vínculos selecionados são sincronizados no mesmo serviço → roteiro, termos, planos e ordens escolhidos passam a pertencer ao evento. | service — `eventos/services.py:24`; `eventos/services.py:38`; `eventos/services.py:57` | `eventos/tests/test_identificacao_service.py` |
| RN-035 | Arquivo de solicitação não é PDF → serviço tenta conversão; PDFs são anexados ao evento → documento solicitado fica privado e associado ao evento. | service/model — `eventos/services.py:72`; `eventos/services.py:97`; `eventos/models.py:228` | `eventos/tests/test_solicitacao_upload.py` |
| RN-036 | Evento recebe ofício/participantes e ainda não tem termo genérico adequado → `garantir_termo_automatico` cria/sincroniza termo automático → autorização pode ser derivada do evento. | service — `eventos/services.py:119` | `eventos/tests/test_document_seed.py` |
| RN-037 | Evento é excluído → anexos próprios são removidos; roteiro ainda usado em outro contexto é desvinculado em vez de destruído → documentos privados não ficam órfãos e reutilização é preservada. | service/model — `eventos/services.py:139`; `eventos/models.py:331` | `eventos/tests/test_excluir_evento.py` |
| RN-038 | Evento está cancelado → apresentação/fluxo usa variante de perigo e ações de cancelamento/reativação seguem condição do status → cancelamento é estado próprio, não exclusão. | view — `eventos/views.py:399`; `eventos/views.py:491` | rotas de evento |
| RN-039 | Modelo de motivo é marcado padrão → apenas o padrão da mesma área é desmarcado; nome/texto são normalizados → uma área não afeta outra. | model — `eventos/models.py:250`; `eventos/models.py:303` | `eventos/tests/test_vazamento_entre_areas.py` |
| RN-040 | Anexo de evento é criado → tipo deve ser convite, ofício solicitante, comprovante ou outro; upload passa por validador privado → arquivo não é exposto como mídia pública irrestrita. | model/upload validator — `eventos/models.py:317`; `core/uploads.py:1` | `eventos/tests/test_solicitacao_upload.py` |

### D. Roteiros, rotas e cálculo de diárias

| ID | Condição → comportamento → resultado | Camada/evidência | Teste/rota |
|---|---|---|---|
| RN-041 | Roteiro é criado → status documental começa RASCUNHO e pode chegar a FINALIZADO; origem pode ser EVENTO ou AVULSO → roteiro existe independentemente de documento. | model — `roteiros/models.py:14` | `roteiros/tests/test_roteiros_base.py` |
| RN-042 | Cálculo de rota evolui → estados são pendente, calculada, manual, erro e desatualizada; fonte é OpenRouteService, manual ou cache → status da rota é separado do status do roteiro. | model — `roteiros/models.py:26` | `roteiros/tests/test_trecho_route_service.py` |
| RN-043 | Trechos são persistidos → tipo é IDA ou RETORNO; ordem é única por roteiro/tipo e período deve ser ordenado → sequência documental determinística. | model/constraints — `roteiros/models.py:356`; `roteiros/models.py:427` | `core/tests/test_colecoes_ordenadas_db08.py` |
| RN-044 | Trechos mudam → datas agregadas do roteiro são recalculadas a partir da coleção → período principal acompanha a rota salva. | service — `roteiros/services/editor_persistence.py:42` | `roteiros/tests/test_gravador_do_editor_be13.py` |
| RN-045 | Destino é classificado → Brasília/DF recebe BRASILIA; capital conhecida recebe CAPITAL; vazio ou demais recebe INTERIOR → faixa tarifária determinada. | cálculo — `roteiros/services/diarias.py:208` | `roteiros/tests/test_diarias_capitais.py` |
| RN-046 | Base geográfica possui capitais → ela prevalece; UFs ausentes usam mapa de segurança → banco recém-criado não transforma capitais em interior. | cálculo/selector — `roteiros/services/diarias.py:180` | `roteiros/tests/test_diarias_capitais.py` |
| RN-047 | Roteiro contém várias paradas → BRASÍLIA prevalece; senão qualquer capital eleva o tipo agregado a CAPITAL; caso contrário INTERIOR → resumo usa maior faixa encontrada. | cálculo — `roteiros/services/diarias.py:221` | `roteiros/tests/test_diarias_capitais.py` |
| RN-048 | Cálculo começa → data da saída mais antiga escolhe uma única vigência para todo o roteiro → travessia de mudança de tabela não mistura valores. | cálculo — `roteiros/services/diarias.py:138`; `roteiros/services/diarias.py:313` | `roteiros/tests/test_diarias_vigencia.py` |
| RN-049 | Não existe vigência para data/faixa → valores históricos internos são usados como último recurso → calculadora continua disponível, mas resultado sinaliza linha de tabela ausente. | cálculo — `roteiros/services/diarias.py:96` | `roteiros/tests/test_diarias_vigencia.py` |
| RN-050 | Período tem duração ≤ 0 → erro; parada instantânea entre destinos é ignorada sem invalidar o roteiro → apenas permanências positivas faturam. | cálculo — `roteiros/services/diarias.py:238`; `roteiros/services/diarias.py:344` | `roteiros/tests/test_diarias_limites_periodo.py` |
| RN-051 | Sobra após dias inteiros é até 6h / >6–8h / >8–12h / >12h → complemento é 0% / 15% / 30% / 100% → escada depende da duração, não da virada do calendário. | cálculo — `roteiros/services/diarias.py:238` | `roteiros/tests/test_diarias_limites_periodo.py` |
| RN-052 | Marcadores de destino são processados → primeiro período começa na saída da sede; seguintes começam na chegada ao destino; terminam na chegada ao próximo/à sede → tempo entre destinos é tarifado pelo local de permanência anterior. | cálculo — `roteiros/services/diarias.py:302`; `roteiros/services/diarias.py:325` | `roteiros/tests/test_diarias_limites_periodo.py` |
| RN-053 | Parada intermediária coincide com a sede → período é ignorado; retorno final à sede prolonga trecho anterior e não abre faixa própria → volta para casa não é tarifada como estadia na sede. | cálculo — `roteiros/services/diarias.py:352`; `roteiros/services/diarias.py:420` | `roteiros/tests/test_diarias_limites_periodo.py` |
| RN-054 | Períodos consecutivos encostados têm a mesma faixa → fundem-se em um trecho tarifário → sobra/complemento é contado uma vez por trecho, não por cidade. | cálculo — `roteiros/services/diarias.py:401` | `roteiros/tests/test_diarias_service.py` |
| RN-055 | Valor do trecho é calculado → dias inteiros × 24h + no máximo um complemento, multiplicado pela quantidade de servidores → total monetário e composição estruturada são produzidos. | cálculo — `roteiros/services/diarias.py:435` | `roteiros/tests/test_db13_composicao_diarias.py` |
| RN-056 | Resultado é persistido no roteiro → quantidade, valor numérico, texto por extenso e componentes de diária são sincronizados → documento e auditoria usam snapshot estruturado. | service/model — `roteiros/services/editor_persistence.py:126`; `roteiros/models.py:227` | `roteiros/tests/test_db13_composicao_diarias.py` |
| RN-057 | Roteiro tenta calcular sem datas/horas suficientes → `ValueError("Preencha datas e horas para calcular.")` → operador deve completar período. | cálculo — `roteiros/services/diarias.py:310` | testes de editor/diárias |
| RN-058 | Dados de rota externa não estão disponíveis ou rota é manual → estado/fonte manual preserva métricas informadas; falha externa vira status ERRO/desatualizada, sem apagar roteiro → fluxo alternativo existe. | routing services/model — `roteiros/services/routing/trecho_route_service.py:1`; `roteiros/models.py:26` | `roteiros/tests/test_trecho_route_service.py` |

### E. Ofícios

| ID | Condição → comportamento → resultado | Camada/evidência | Teste/rota |
|---|---|---|---|
| RN-059 | Ofício é criado → status padrão RASCUNHO, data local automática e número/ano reservados → rascunho já ocupa numeração. | model/service — `oficios/models.py:25`; `oficios/models.py:56`; `oficios/services.py:606` | `oficios/tests/test_numeracao.py`; rota `/oficios/novo/` |
| RN-060 | Numeração automática é pedida → menor lacuna liberada por exclusão é usada; sem lacuna, maior número + 1 → saltos nunca usados manualmente não viram sugestão. | model/service — `oficios/models.py:215`; `oficios/services.py:636` | `oficios/tests/test_models.py`; `oficios/tests/test_services.py` |
| RN-061 | Duas reservas concorrentes colidem → serviço usa até 3 tentativas/savepoint e recalcula → `IntegrityError` de disputa não derruba imediatamente a transação externa. | service — `oficios/services.py:660` | `oficios/tests/test_numbering_concurrency.py` |
| RN-062 | Número/ano preenchidos repetem a mesma área → constraint rejeita; áreas distintas têm sequências independentes → unicidade territorial anual. | model — `oficios/models.py:187` | `oficios/tests/test_recorte_por_area.py` |
| RN-063 | Ofício numerado é excluído → número vira `OficioNumeroLacuna`; edição, cancelamento ou retificação não liberam o anterior → reaproveitamento só nasce de exclusão. | service/model — `oficios/services.py:1013`; `oficios/models.py:371` | `oficios/tests/test_models.py`; `oficios/tests/test_services.py` |
| RN-064 | Etapa de dados é salva como rascunho ou continuar → modelo de motivo pode preencher texto e serviço avalia completude → status automático RASCUNHO/GERADO conforme ação e campos. | service — `oficios/services.py:407`; `oficios/services.py:417`; `oficios/services.py:427` | `oficios/tests/test_abas.py` |
| RN-065 | Custeio é OUTRA_INSTITUIÇÃO → observação/nome da instituição torna-se obrigatório; demais tipos não exigem → documento não sai sem identificar custeador externo. | form/service — `oficios/forms.py:1`; `oficios/services.py:699` | `oficios/tests/test_wizard_dados_viajantes.py` |
| RN-066 | Transporte usa viatura cadastrada ou manual → dados efetivos vêm da FK ou campos manuais; modo manual exige dados correspondentes → documento usa fonte selecionada. | model/form/service — `oficios/models.py:87`; `oficios/services.py:547`; `oficios/services.py:589` | `oficios/tests/test_wizard_transporte.py` |
| RN-067 | Motorista é servidor ou manual → campos efetivos mudam conforme modo; CPF manual é normalizado/validado → identificação do motorista acompanha a escolha. | model/form/service — `oficios/models.py:140`; `oficios/services.py:547` | `oficios/tests/test_wizard_transporte.py`; `oficios/tests/test_docxtpl_motorista.py` |
| RN-068 | Motorista é “carona”/externo e exige protocolo → pendência específica é produzida quando protocolo está ausente/inválido → geração documental é bloqueada até correção. | validation service — `oficios/services.py:731`; `oficios/services.py:748` | `oficios/tests/test_protocolo_obrigatorio_novo.py` |
| RN-069 | Evento já tem roteiro padrão → serviço pode vinculá-lo sem cópia; edição própria materializa/revincula rascunho → ofício alterna entre reutilização e roteiro próprio preservando origem. | service — `oficios/services.py:128`; `oficios/services.py:172`; `oficios/services.py:215` | `oficios/tests/test_roteiro_do_oficio_be12.py`; `oficios/tests/test_wizard_roteiro_diarias.py` |
| RN-070 | Ofício tenta gerar/finalizar documento → validação agrega pendências de dados, motorista/protocolo, transporte, roteiro e justificativa → usuário é redirecionado à etapa de correção. | service — `oficios/services.py:699`; `oficios/services.py:794`; `oficios/services.py:826` | `oficios/tests/test_services.py` |
| RN-071 | Documento do ofício é solicitado em DOCX/PDF → payload canônico é montado e resposta passa pelo núcleo; PDF tenta cadeia de motores/cache → download ou erro tratado. | service — `oficios/services.py:847`; `oficios/services.py:1065` | `oficios/tests/test_views.py`; `oficios/tests/test_documento_pdf_erro_amigavel.py` |
| RN-072 | Documento é gerado com sucesso → artefato/cache é persistido e status pode refletir GERADO → gerações repetidas equivalentes podem reutilizar saída. | service/document cache — `oficios/services.py:61`; `documentos/services/document_cache.py:77` | `documentos/tests/test_cache_de_documento.py` |
| RN-073 | Ofício é cancelado → flag/motivo/data de cancelamento são gravados; cancelamento não exclui nem libera número → histórico permanece. | service/model — `oficios/services.py:1027`; `core/models.py:16` | `oficios/tests/test_services.py`; `oficios/tests/test_views.py` |
| RN-074 | Ofício é retificado/desfeito → marcadores de retificação mudam sem trocar identidade/número → fluxo alternativo é reversível. | service — `oficios/services.py:1034`; `oficios/services.py:1044` | `oficios/tests/test_services.py`; `oficios/tests/test_views.py` |
| RN-075 | Ofício é marcado complementar/desfeito → flag complementar alterna → apresentação/documento distingue complemento sem criar nova regra de numeração. | service — `oficios/services.py:1050`; `oficios/services.py:1059` | testes de ofício |
| RN-076 | Exclusão encontra vínculos protegidos → `OficioVinculadoError` bloqueia e explica; se permitido, limpeza documental/número ocorre no serviço → cascata não é deixada à view. | service — `oficios/services.py:113`; `oficios/services.py:1013` | `oficios/tests/test_services.py`; `oficios/tests/test_views.py` |

### F. Justificativas

| ID | Condição → comportamento → resultado | Camada/evidência | Teste/rota |
|---|---|---|---|
| RN-077 | Prazo institucional existe → usa `ConfiguracaoSistema.prazo_justificativa_dias`; ausência cai em 10 dias → regra tem fallback. | service/model — `justificativas/services.py:22`; `cadastros/models.py:525` | `justificativas/tests/test_services.py` |
| RN-078 | Ofício tem primeira saída → antecedência é diferença entre a data da saída e `oficio.data_criacao`; abaixo do prazo torna justificativa obrigatória → snapshot guarda dias, prazo e primeira saída. | service/model — `justificativas/services.py:31`; `justificativas/services.py:55`; `justificativas/models.py:122` | `justificativas/tests/test_services.py` |
| RN-079 | Justificativa obrigatória está ausente/incompleta → validação do ofício impede geração/finalização → operador volta à etapa de justificativa. | service integration — `justificativas/services.py:220`; `oficios/services.py:783` | `oficios/tests/test_wizard_justificativa.py` |
| RN-080 | Justificativa é salva como rascunho/continuar → texto é normalizado e status muda para RASCUNHO/FINALIZADA conforme ação/completude → uma justificativa por ofício. | model/service — `justificativas/models.py:95`; `justificativas/services.py:170` | `justificativas/tests/test_forms_services_layer.py` |
| RN-081 | Modelo reutilizável é padrão → somente outro padrão da mesma área é desmarcado; nomes podem repetir entre áreas, não dentro da mesma → isolamento de textos institucionais. | model/constraints — `justificativas/models.py:41`; `justificativas/models.py:81` | `justificativas/tests/test_modelo_por_area.py` |
| RN-082 | Justificativa não é exigida → etapa pode seguir sem texto finalizado; quando exigida, texto passa a obrigatório → fluxo alternativo explícito. | service — `justificativas/services.py:67`; `justificativas/services.py:228` | `justificativas/tests/test_services.py` |

### G. Termos de autorização

| ID | Condição → comportamento → resultado | Camada/evidência | Teste/rota |
|---|---|---|---|
| RN-083 | Termo é salvo sem área explícita → herda do evento, depois do ofício, depois da área corrente → persistência sempre territorial. | model — `termos/models.py:100` | `termos/tests/test_termo_cadastro.py` |
| RN-084 | Datas próprias existem → período usa início/fim; sem elas, cai no roteiro do ofício; fim ausente equivale ao início → exibição pode ser um dia ou intervalo. | model — `termos/models.py:118`; `termos/models.py:146` | `termos/tests/test_termo_cadastro.py` |
| RN-085 | Data final é anterior à inicial → constraint `termo_periodo_ordenado` rejeita → período assinado não fica invertido. | model/constraint — `termos/models.py:86` | `termos/migrations/0009_termo_periodo_ordenado.py:34` |
| RN-086 | Destino próprio existe → usa cidade/UF + extras sem duplicar; sem cidade usa estado; sem ambos cai no primeiro destino do ofício → termo funciona como cadastro independente ou derivado. | model — `termos/models.py:126` | `termos/tests/test_termo_cadastro.py` |
| RN-087 | Servidores próprios selecionados → eles prevalecem; senão usa servidores de termo do ofício; senão servidores dos ofícios do evento → lote encontra participantes efetivos com fallback. | model — `termos/models.py:160`; `termos/models.py:185` | `termos/tests/test_downloads_do_termo.py` |
| RN-088 | Termo genérico do evento agrega servidores → IDs são deduplicados e cada servidor mantém ofício/viatura de origem quando possível → documentos individuais não perdem contexto. | model/service — `termos/models.py:220`; `termos/services.py:411` | `termos/tests/test_downloads_do_termo.py` |
| RN-089 | Viatura própria existe → prevalece; senão usa a do ofício; inexistente gera variante sem viatura → modelo DOCX muda conforme transporte. | model/service — `termos/models.py:213`; `termos/services.py:394` | `documentos/resources/termo_autorizacao_automatico.docx`; `..._sem_viatura.docx` |
| RN-090 | Geração é individual/lote/genérica/selecionados → serviço escolhe servidores e produz um ou vários documentos; PDFs podem ser fundidos → download consolidado. | service — `termos/services.py:452`; `termos/services.py:531`; `termos/services.py:678` | `termos/tests/test_downloads_do_termo.py` |
| RN-091 | Existe PDF assinado do termo → versão assinada prevalece; senão usa PDF gerado → consumo sempre prefere manifestação assinada. | service — `termos/services.py:550`; `termos/services.py:604` | `termos/tests/test_assinatura_manual.py` |

### H. Planos de trabalho

| ID | Condição → comportamento → resultado | Camada/evidência | Teste/rota |
|---|---|---|---|
| RN-092 | Plano é criado → status RASCUNHO; geração bem-sucedida marca GERADO → estado é simples e separado de cancelamento. | model/service — `planos_trabalho/models.py:235`; `planos_trabalho/services.py:1295` | testes de geração de plano |
| RN-093 | Numeração do plano é necessária → transação bloqueia configuração, reconcilia contador com maior número do banco e zera ao mudar o ano → número único por área/ano. | service/model — `planos_trabalho/services.py:1302`; `cadastros/models.py:568` | `planos_trabalho/tests/test_numbering_concurrency.py`; `planos_trabalho/tests/test_models.py` |
| RN-094 | Coordenador administrativo/operacional usa modo SERVIDOR ou MANUAL → FK ou nome/cargo/gênero manual compõem o texto → plano aceita pessoa cadastrada ou externa. | model/form — `planos_trabalho/models.py:243`; `planos_trabalho/forms.py:375` | testes de formulário de plano |
| RN-095 | Programa “outros” é escolhido → texto manual correspondente é exigido; programa inválido é rejeitado → documento sempre identifica programa. | form/service — `planos_trabalho/forms.py:279`; `planos_trabalho/services.py:109` | testes de identificação |
| RN-096 | Campos automáticos de contextualização/coordenação/consideração estão ativos → textos padrão são regenerados; se usuário desativa auto, texto manual é preservado → automação não sobrescreve edição deliberada. | service/model — `planos_trabalho/services.py:151`; `planos_trabalho/models.py:316` | testes de textos padrão |
| RN-097 | Plano tem atividades selecionadas → metas, atividades, recursos e unidade móvel são montados na ordem do catálogo; presets exigem pelo menos uma atividade → texto documental derivado. | service/form — `planos_trabalho/services.py:775`; `planos_trabalho/services.py:842`; `planos_trabalho/forms.py:867` | `planos_trabalho/tests/test_services.py`; `planos_trabalho/tests/test_wizard_v2.py` |
| RN-098 | Efetivo contém cargo/unidade/quantidade → combinação é única por plano/evento e quantidade inicia em 1 → resumo pluraliza e soma equipe. | model/service — `planos_trabalho/models.py:712`; `planos_trabalho/services.py:320` | testes de efetivo |
| RN-099 | Plano comum calcula diárias → reutiliza motor central por período/destino e multiplica pelo efetivo → snapshot de composição, unitário e total é salvo. | service — `planos_trabalho/services.py:393`; `planos_trabalho/services.py:454` | testes de diárias do plano |
| RN-100 | Plano multievento possui eventos → cada evento tem período/destino/efetivo próprios; diárias são calculadas por evento e combinadas → documento multievento recebe blocos separados e total. | service/model — `planos_trabalho/services.py:612`; `planos_trabalho/services.py:646`; `planos_trabalho/models.py:753` | testes multievento |
| RN-101 | Scratchpad contém dados e usuário adiciona/sincroniza → vira `EventoPlano`; editar carrega evento no scratchpad; remover exclui o evento e seus efetivos → editor multievento tem ciclo explícito. | service — `planos_trabalho/services.py:930`; `planos_trabalho/services.py:1047`; `planos_trabalho/services.py:1068` | testes de scratchpad |
| RN-102 | Geração é solicitada → três avaliadores cobrem identificação, efetivo/diárias e atividades; pendências bloqueiam → só documento completo é emitido. | service — `planos_trabalho/services.py:1108`; `planos_trabalho/services.py:1138` | testes de geração de plano |
| RN-103 | Plano é multievento → template `plano_trabalho_multievento.docx`; caso contrário `plano_trabalho.docx` → saída segue a estrutura do plano. | service/resources — `planos_trabalho/services.py:1172`; `documentos/resources/plano_trabalho_multievento.docx` | golden tests em `documentos/tests/golden/` |

### I. Ordens de serviço

| ID | Condição → comportamento → resultado | Camada/evidência | Teste/rota |
|---|---|---|---|
| RN-104 | Ordem escolhe tipo de necessidade → opções incluem padrão, operação com retorno posterior, caminhão, micro-ônibus e cerimonial antecipado → template/contexto pode variar por necessidade. | model — `ordens_servico/models.py:24` | testes de ordem |
| RN-105 | Servidores da ordem recebem função → escolhas são condução, técnico, apoio, coordenação e preparação; mapa JSON guarda função por servidor → documento descreve atribuições. | model/form — `ordens_servico/models.py:31`; `ordens_servico/forms.py:400` | testes de formulário OS |
| RN-106 | Função é informada para servidor fora da seleção ou função inválida → limpeza do formulário rejeita/corrige → JSON não referencia equipe inexistente. | form — `ordens_servico/forms.py:400` | testes de formulário OS |
| RN-107 | Período da ordem é preenchido → fim não pode anteceder início; destinos extras são normalizados → ordem mantém período/documento coerente. | model/form — `ordens_servico/models.py:179`; `ordens_servico/forms.py:371` | `ordens_servico/migrations/0011_os_periodo_ordenado.py:31` |
| RN-108 | Ordem precisa de número → sequência anual por área é única; lacunas liberadas por exclusão são registradas → numeração suporta concorrência/reuso controlado. | model — `ordens_servico/models.py:179`; `ordens_servico/models.py:326` | `ordens_servico/tests/test_numeracao.py` |
| RN-109 | Ordem possui ofícios vinculados → contexto reutiliza participantes, transporte, roteiro/evento; sem vínculo, campos próprios sustentam o documento → entidade pode ser derivada ou independente. | form/service — `ordens_servico/forms.py:50`; `ordens_servico/services.py:79` | rotas `ordens_servico:baixar_docx/pdf` |
| RN-110 | Documento da ordem é solicitado → DOCX/PDF usam template padrão ou `ordem_servico_modelos.docx` conforme regra de template → resposta passa por cache/persistência documental. | service — `ordens_servico/services.py:53`; `ordens_servico/services.py:143`; `ordens_servico/services.py:199` | golden `documentos/tests/golden/ordem_servico*.txt` |

### J. Prestação de contas, relatório técnico, diário e assinatura

| ID | Condição → comportamento → resultado | Camada/evidência | Teste/rota |
|---|---|---|---|
| RN-111 | Ofício muda para GERADO ou sua equipe muda → signals criam/sincronizam `PrestacaoContas` e linhas por servidor → módulo nasce automaticamente do ofício. | signal — `prestacoes_contas/signals.py:6`; `prestacoes_contas/signals.py:70` | testes de signals/prestações |
| RN-112 | Servidor começa a editar → status individual passa de pendente para em preenchimento; opções adicionais são enviada, aprovada e reprovada → acompanhamento é por servidor. | model/service — `prestacoes_contas/models.py:179`; `prestacoes_contas/models.py:337` | testes de componentes de prestação |
| RN-113 | Arquivar/finalizar é acionado → cada POST alterna a flag e grava/remove timestamp; flags são independentes do `status` → duplo acionamento desfaz a ação. | model/view — `prestacoes_contas/models.py:321`; `prestacoes_contas/views.py:483`; `prestacoes_contas/views.py:495` | rotas de arquivar/finalizar |
| RN-114 | Finalizar é acionado → não há pré-condição documental no endpoint, apenas alternância → conclusão é decisão operacional. | view — `prestacoes_contas/views.py:495` | teste de finalização de prestação |
| RN-115 | Servidor sai da equipe sem dados coletados → linha é apagada; com dados, recebe `removida_em`; ao voltar, marcador é limpo → trabalho já feito pode reaparecer. | model — `prestacoes_contas/models.py:342`; `prestacoes_contas/models.py:391`; `prestacoes_contas/models.py:408` | testes de sincronização de equipe |
| RN-116 | Exclusão de servidor é avaliada → prova irrefazível é apenas comprovante/anexo, assinatura ou número de solicitação → estados refazíveis não bloqueiam cadastro inteiro. | model/service — `prestacoes_contas/models.py:365`; `cadastros/services.py:255` | `cadastros/tests/test_exclusao_de_servidor.py` |
| RN-117 | Valor recebido é informado → deve ser positivo e nunca superar o valor liberado por servidor; menor é aceito com observação separada → relatório mostra override (ex.: saque sem centavos). | service/model/constraint — `prestacoes_contas/services.py:213`; `prestacoes_contas/services.py:240`; `prestacoes_contas/models.py:210` | testes de diária recebida/override |
| RN-118 | Data de liberação e prazo de saque são informados → datas ISO são validadas e prazo não pode anteceder liberação → lote/autosave usam mesma persistência. | service/model constraint — `prestacoes_contas/solicitacao_services.py:72`; `prestacoes_contas/models.py:283` | testes de solicitação/autosave |
| RN-119 | Número de solicitação muda → recarimbo do ofício assinado é agendado depois do commit e deduplicado por ofício → PDF não fica com número antigo nem é refeito N vezes. | service — `prestacoes_contas/solicitacao_services.py:25`; `prestacoes_contas/solicitacao_services.py:113` | testes de carimbo |
| RN-120 | Anexo assinado é substituído/excluído → linha muda dentro da transação e arquivos antigos são apagados somente em `on_commit` → rollback não deixa linha apontando para arquivo destruído. | service — `prestacoes_contas/anexo_services.py:51`; `prestacoes_contas/anexo_services.py:63`; `prestacoes_contas/anexo_services.py:114` | `prestacoes_contas/test_anexos_transacao_be14.py` |
| RN-121 | Ofício assinado recebe carimbos → arquivo original cru é preservado; posições são por página/servidor, únicas, e ajuste manual não é recalculado → recarimbo sempre parte da origem. | model — `prestacoes_contas/models.py:451`; `prestacoes_contas/models.py:481` | testes de carimbo |
| RN-122 | RT é aberto/salvo → valores padrão são derivados do ofício/roteiro/configuração; texto e overrides são persistidos numa transação → autosave e POST tradicional têm mesma regra. | services — `prestacoes_contas/services.py:300`; `prestacoes_contas/rt_services.py:189`; `prestacoes_contas/rt_services.py:225` | testes de RT/autosave |
| RN-123 | RT é gerado → template `relatorio-tecnico.docx` recebe contexto do ofício, servidor, custos e textos; formatos DOCX/PDF disponíveis → arquivo é individual por servidor. | service/resource — `prestacoes_contas/services.py:379`; `prestacoes_contas/services.py:424`; `prestacoes_contas/services.py:432` | `documentos/resources/relatorio-tecnico.docx` |
| RN-124 | Prestação precisa de diário → roteiro original é clonado somente quando ajuste é necessário; diferenças são comparadas → alterações de prestação não modificam roteiro do ofício. | service — `prestacoes_contas/diario_services.py:41`; `prestacoes_contas/diario_services.py:53`; `prestacoes_contas/diario_services.py:137` | testes de diário/roteiro ajustado |
| RN-125 | Diário sincroniza trechos → reaproveita linhas para preservar KM/abastecimento e impõe ordem única → mudança de roteiro não apaga edição compatível. | service/model — `prestacoes_contas/diario_services.py:276`; `prestacoes_contas/models.py:667` | testes de diário |
| RN-126 | KM inicial/final são preenchidos → final deve ser ≥ inicial; igualdade é aceita → rodagem negativa não entra no diário. | constraint — `prestacoes_contas/models.py:703` | testes de constraints DB-07 |
| RN-127 | Motorista do diário é alterado → pode manter o do ofício, escolher servidor do mesmo ofício ou referenciar outro ofício; mudança é local à prestação → ofício original não muda. | model/service — `prestacoes_contas/models.py:556`; `prestacoes_contas/diario_services.py:733` | rotas de motorista do diário |
| RN-128 | Viatura do diário é alterada → pode manter ofício, escolher cadastro ou preencher manualmente; alteração é independente do motorista → cabeçalho do diário usa override local. | model/service — `prestacoes_contas/models.py:576`; `prestacoes_contas/diario_services.py:395` | testes de diário |
| RN-129 | Diário é exportado → XLSX usa `diario_bordo.xlsx`; PDF deriva da planilha por motor disponível → formatos são XLSX e PDF, não DOCX/ODT. | service/resource — `prestacoes_contas/diario_services.py:544`; `prestacoes_contas/diario_services.py:548`; `prestacoes_contas/diario_services.py:554` | `documentos/resources/diario_bordo.xlsx` |
| RN-130 | PDF final consolidado é solicitado → pendências exigem peças obrigatórias (incluindo documentos assinados conforme fluxo); se houver pendência, geração é recusada → usuário recebe lista acionável. | service/view — `prestacoes_contas/services.py:612`; `prestacoes_contas/services.py:720`; `prestacoes_contas/views.py:562` | rota `consolidado_download` |
| RN-131 | Anexo para consolidação é PDF/PNG/JPG/JPEG → imagem é convertida a PDF; outros formatos ou PDF sem páginas falham → mesclagem só aceita conteúdo legível. | service — `prestacoes_contas/services.py:478`; `prestacoes_contas/services.py:524`; `prestacoes_contas/services.py:549` | testes de anexos/consolidado |
| RN-132 | Link de assinatura é emitido → exige signatário com CPF válido, snapshot do PDF e expiração (padrão 7 dias); `forcar` substitui emissão anterior → link público é tokenizado. | service — `prestacoes_contas/assinatura_services.py:49`; `prestacoes_contas/assinatura_services.py:93`; `prestacoes_contas/assinatura_services.py:123` | testes de assinatura |
| RN-133 | Acesso público informa CPF → identidade só confirma se coincidir com CPF esperado; tentativas são limitadas por cache/IP/token → documento não é liberado apenas pelo token. | service/view — `prestacoes_contas/assinatura_services.py:187`; `prestacoes_contas/assinatura_views.py:63`; `prestacoes_contas/assinatura_views.py:148` | rotas públicas de assinatura |
| RN-134 | Assinatura é aplicada → modo pode ser fonte ou desenho; posição/página são validadas, PDF recebe carimbo e SHA-256 da origem é guardado → versão assinada fica vinculada ao snapshot exibido. | service/model — `prestacoes_contas/assinatura_services.py:272`; `prestacoes_contas/models.py:724` | testes de assinatura |
| RN-135 | Documento tem versão assinada válida → download usa assinada; senão gera a origem atual → assinatura prevalece sem eliminar fallback. | service — `prestacoes_contas/assinatura_services.py:359`; `prestacoes_contas/assinatura_services.py:366` | testes de assinatura |

### K. Núcleo documental e arquivos gerados

| ID | Condição → comportamento → resultado | Camada/evidência | Teste/rota |
|---|---|---|---|
| RN-136 | Tipo documental é solicitado → registro/template precisa existir e formato ser compatível; duplicidade de registro falha → geração não aceita tipo arbitrário. | registry/facade — `documentos/services/registry.py:8`; `documentos/services/facade.py:117` | `documentos/tests/test_registry.py` |
| RN-137 | Caminho de template é resolvido → deve permanecer dentro de `documentos/resources` → traversal/caminho externo é rejeitado. | resource resolver — `documentos/services/resources_paths.py:12` | `documentos/tests/test_templates.py` |
| RN-138 | Payload não contém placeholder obrigatório ou saída mantém placeholder → geração levanta erro explícito → marcadores não vazam para documento final. | validator — `documentos/services/placeholders.py:16`; `documentos/services/placeholders.py:30` | `documentos/tests/test_placeholders.py` |
| RN-139 | DOCX é solicitado → `docxtpl` preenche template e retorna bytes; pacote/template ausente falha explicitamente → nenhum pacote é instalado durante request. | renderer/adapter — `documentos/services/adapters/docxtpl_render.py:8`; `documentos/services/renderers/docx_renderer.py:10` | `documentos/tests/test_renderers.py` |
| RN-140 | PDF em `auto` é solicitado → cadeia varia por SO e disponibilidade: Word/LibreOffice/unoserver/WeasyPrint/simple conforme preferência/configuração → melhor motor disponível é tentado. | engine resolver — `documentos/services/pdf_engine.py:162`; `documentos/services/pdf_engine.py:287` | `documentos/tests/test_pdf_engine.py` |
| RN-141 | Motor explícito falha e fallback automático está ligado → motores seguintes são tentados; sem motor, erro inclui orientação → falha não some em silêncio. | facade/engine — `documentos/services/facade.py:170`; `documentos/services/facade.py:274` | `documentos/tests/test_download_errors.py` |
| RN-142 | PDF simples não está permitido → `simple` não entra na cadeia; quando permitido, produz layout mínimo → fidelidade reduzida é fallback deliberado. | engine/adapter — `documentos/services/pdf_engine.py:91`; `documentos/services/adapters/simple_pdf_fallback.py:92` | `documentos/tests/test_simple_pdf_fallback.py` |
| RN-143 | Conteúdo+template+formato+versão equivalentes geram chave idêntica → artefato válido pode ser reutilizado; mudança no payload/template invalida → cache é determinístico. | cache — `documentos/services/document_cache.py:77`; `documentos/services/document_cache.py:108` | `documentos/tests/test_document_cache.py` |
| RN-144 | Geração assíncrona é pedida → job fica queued → processing → complete/error; pedido duplicado pode reutilizar `dedupe_key` → frontend recebe 202/status/result. | model/service — `documentos/models.py:130`; `documentos/services/async_generation.py:271`; `documentos/services/async_generation.py:344` | `documentos/tests/test_async_generation.py` |
| RN-145 | Resultado assíncrono pertence a outra área/usuário ou não concluiu → acesso é negado/202; concluído entrega arquivo → isolamento vale também para downloads. | service/access — `documentos/services/async_generation.py:246`; `documentos/services/access.py:1` | `documentos/tests/test_recorte_por_area_fatia4.py` |
| RN-146 | PDF inline recebe Range válido → retorna faixa 206; Range inválido recebe 416; arquivo completo recebe 200 → visualizador suporta streaming. | response service — `documentos/services/pdf_streaming.py:46`; `documentos/services/pdf_streaming.py:93` | `documentos/tests/test_pdf_streaming.py` |
| RN-147 | Arquivo assinado é anexado a artefato → upload é validado e versão fica associada; remoção limpa vínculo → assinatura manual pode substituir consumo do gerado. | persistence — `documentos/services/persistence.py:120`; `documentos/services/persistence.py:132`; `documentos/services/persistence.py:171` | `documentos/tests/test_assinatura_manual.py` |
| RN-148 | Versão documental assinada é criada → conteúdo/hash/revogação tornam-se imutáveis; versão não pode ser excluída → cadeia de prova é preservada. | model — `documentos/models.py:192`; `documentos/models.py:226`; `documentos/models.py:245` | testes de assinatura versionada |
| RN-149 | Conversão unoserver recebe URL inválida, serviço indisponível ou saída não-PDF → erro explícito; cache binário por SHA-256 pode reaproveitar conversões idênticas → não retorna bytes inválidos. | adapter — `documentos/services/adapters/libreoffice_pdf.py:113`; `documentos/services/adapters/libreoffice_pdf.py:166` | `documentos/tests/test_unoserver_adapter.py` |
| RN-150 | Arquivos atualmente geráveis e comprovados no repositório → Ofício (DOCX/PDF), Justificativa (DOCX/PDF), Termo (DOCX/PDF, individual/lote), Plano (DOCX/PDF), Ordem (DOCX/PDF), Relatório Técnico (DOCX/PDF), Diário de Bordo (XLSX/PDF), prestação consolidada (PDF) → não foi localizado gerador ODT. | resources/services — `documentos/resources/`; `oficios/services.py:847`; `termos/services.py:531`; `planos_trabalho/services.py:1172`; `ordens_servico/services.py:143`; `prestacoes_contas/services.py:424`; `prestacoes_contas/diario_services.py:548` | golden tests em `documentos/tests/golden/` |

### L. Protocolos/eProtocolo

| ID | Condição → comportamento → resultado | Camada/evidência | Teste/rota |
|---|---|---|---|
| RN-151 | Protocolo local é criado → status começa rascunho e pode evoluir por criado, documentos enviados, aguardando assinatura, tramitação, pendência, concluído, erro ou cancelado → integração mantém estado local próprio. | model — `protocolos/models.py:31` | `protocolos/tests/test_services.py` |
| RN-152 | Origem é escolhida → interna, manual ou eProtocolo real determinam se há criação remota, simples vínculo ou importação → mesmo modelo representa três fluxos. | model/service — `protocolos/models.py:54`; `protocolos/services.py:523`; `protocolos/services.py:569` | testes de serviços |
| RN-153 | Ambiente está em demonstração/mock → dados de treinamento são semeados e operações exibem aviso; mutação real não é feita → treinamento não altera eProtocolo oficial. | service/view — `protocolos/services.py:167`; `protocolos/services.py:173`; `protocolos/views.py:35` | `protocolos/tests/test_eprotocolo_treinamento.py` |
| RN-154 | Operação real mutável é solicitada → exige flags `real` e `confirmado` e configuração controlada → criação/anexo/assinatura/tramitação não ocorre por engano. | service — `protocolos/services.py:368` | `protocolos/tests/test_eprotocolo_real_controlado.py` |
| RN-155 | Usuário não possui permissão de criação de protocolo → views de criar/vincular bloqueiam → integração respeita política própria além do RBAC de área. | permission/view — `protocolos/views.py:113`; `protocolos/permissions.py:1` | testes de views |
| RN-156 | Documento principal tem origem suportada → serviço resolve/genera PDF e envia; anexo adicional cria `ProtocoloDocumento` → estado pode avançar para documentos enviados. | service — `protocolos/services.py:600`; `protocolos/services.py:629`; `protocolos/services.py:1019` | rotas de enviar/anexar |
| RN-157 | Assinatura remota é solicitada → CPF é normalizado, registro local inicia pendente e resultado remoto atualiza status → acompanhamento guarda payload e assinante. | service/model — `protocolos/services.py:702`; `protocolos/models.py:457` | testes de serviços |
| RN-158 | Tramitação é solicitada → destino/CPF/parecer formam a operação e uma linha local registra de/para → histórico de tramitação é preservado. | service/model — `protocolos/services.py:757`; `protocolos/models.py:540` | testes eProtocolo |
| RN-159 | Sincronização encontra pendências abertas → status local vira com pendência; sem pendências e antes “criado”, pode avançar para tramitação → estado local deriva da resposta remota. | service — `protocolos/services.py:806`; `protocolos/services.py:835` | testes de sincronização |
| RN-160 | Protocolo está concluído/cancelado ou inativo → sincronização em lote o exclui do conjunto ativo → polling não processa fluxos terminais. | service/model — `protocolos/services.py:849`; `protocolos/models.py:109` | testes de serviços |
| RN-161 | Toda chamada de integração é registrada → endpoint, método, sucesso/mock, erro e payloads ficam em `ProtocoloLog` → auditoria operacional do conector. | model/service — `protocolos/models.py:587`; `protocolos/services.py:137` | testes de logging de protocolo |

### M. Google Drive e efeitos colaterais

| ID | Condição → comportamento → resultado | Camada/evidência | Teste/rota |
|---|---|---|---|
| RN-162 | Artefato/documento relevante é salvo → signals podem organizar/sincronizar no Drive; falha é registrada e, quando há request, gera mensagem → erro externo não desaparece silenciosamente. | signal/middleware — `integracoes/google_drive/signals.py:1`; `core/middleware.py:51` | `integracoes/google_drive/tests/test_apps.py` |
| RN-163 | Credenciais/tokens são persistidos → migração criptografa tokens e credenciais podem ser por usuário/área → segredo não fica em texto simples de regra. | model/migration — `integracoes/google_drive/models.py:9`; `integracoes/google_drive/migrations/0011_criptografa_tokens.py:1` | `integracoes/google_drive/tests/test_token_encryption.py` |
| RN-164 | Reorganização é iniciada → job começa em andamento e contabiliza eventos processados, avulsos e erros; termina concluída ou erro → execução longa tem progresso persistido. | model/service — `integracoes/google_drive/models.py:37`; `integracoes/google_drive/services.py:1` | `integracoes/google_drive/tests/test_organizer.py` |
| RN-165 | Arquivo sincronizado representa documento/evento → IDs de arquivo/pasta/atalho e flag mock são gravados → modo real e simulado são distinguíveis. | model — `integracoes/google_drive/models.py:97`; `integracoes/google_drive/models.py:124` | testes `test_organizer.py`, `test_dual_org.py` |
| RN-166 | Sincronização falha → `DriveSyncStatus` incrementa tentativas e mantém último erro; tarefas de retry processam pendências → efeito externo é recuperável. | model/tasks — `integracoes/google_drive/models.py:174`; `integracoes/google_drive/tasks.py:1` | `integracoes/google_drive/tests/test_retry_pendencias.py` |
| RN-167 | Registro de origem é excluído → arquivos/atalhos relacionados são removidos conforme signals/serviços → limpeza externa acompanha cascata local quando possível. | signals/services — `integracoes/google_drive/signals.py:1`; `integracoes/google_drive/services.py:1` | `integracoes/google_drive/tests/test_exclusao_cascata.py` |

### N. Regras e capacidades não confirmadas

| ID | Estado | Evidência e limite |
|---|---|---|
| RN-168 | **NÃO CONFIRMADO — ODT** | Não foi localizado arquivo `.odt`, renderer, enum de formato ou rota de geração ODT. Recursos existentes são DOCX/XLSX e saídas DOCX/PDF/XLSX (`documentos/resources/`; `documentos/services/types.py:1`). Não apresentar ODT como capacidade. |
| RN-169 | **NÃO CONFIRMADO — decomposição 70%/30%** | O motor soma diária cheia e percentuais 15/30/100; comentário menciona decomposição oficial hospedagem/alimentação, mas não há campos/saída própria (`roteiros/services/diarias.py:445`). Não apresentar como cálculo implementado. |
| RN-170 | **NÃO CONFIRMADO — congelamento financeiro por documento** | O valor é reobtido pela vigência da data e há componentes persistidos; não foi encontrada cópia imutável universal do valor em cada documento já emitido (`roteiros/services/diarias.py:96`; `roteiros/models.py:227`). Editar vigência antiga pode alterar regeneração; requer validação operacional antes de afirmar imutabilidade histórica. |
| RN-171 | **NÃO CONFIRMADO — status FINALIZADO do Ofício como fluxo ativo** | Choice existe (`oficios/models.py:28`), mas serviços atuais usam RASCUNHO/GERADO e o código trata FINALIZADO como compatibilidade/legado. Não descrever etapa operacional própria sem navegação real. |
| RN-172 | **NÃO CONFIRMADO — assinatura digital dos documentos gerais** | `AssinaturaConfiguracao` escolhe nomes/cargos para documento (`cadastros/models.py:690`), mas assinatura eletrônica efetiva lida neste eixo está concentrada em RT/Diário e versões anexadas. Não confundir configuração de assinante com assinatura digital. |
| RN-173 | **NÃO CONFIRMADO — API externa de rotas sempre disponível** | Existe provider OpenRouteService e fluxo manual/cache (`roteiros/services/routing/openrouteservice.py:1`; `roteiros/models.py:39`), mas disponibilidade depende de configuração/rede. Manual deve apresentar o fallback manual e evitar prometer cálculo online. |
| RN-174 | **NÃO CONFIRMADO — efeito normativo dos PDFs de referência** | Os PDFs fornecidos são referências visuais/estruturais segundo o pedido colado. Nenhuma regra foi promovida deles sem correspondência no código. |

### Contagens desta matriz

- **167 regras confirmadas** (`RN-001` a `RN-167`).
- **7 limitações/hipóteses não confirmadas** (`RN-168` a `RN-174`), fora da contagem de regras confirmadas.
- **167 regras confirmadas por código** (`RN-001` a `RN-167`).
- Distribuição confirmada: 12 transversais; 19 cadastros/configuração; 9 eventos; 18 roteiros/diárias; 18 ofícios; 6 justificativas; 9 termos; 12 planos; 7 ordens; 25 prestação/RT/diário/assinatura; 15 núcleo documental; 11 protocolos; 6 Google Drive.
- **8 famílias de arquivos gerados confirmadas**: Ofício, Justificativa, Termo, Plano de Trabalho, Ordem de Serviço, Relatório Técnico, Diário de Bordo e Prestação Consolidada.
- **3 formatos de saída confirmados**: DOCX, PDF e XLSX. ODT: não confirmado.

### Cobertura técnica lida

- Camadas: modelos, forms, views, services, selectors/managers, signals, middleware, context processors, validators de upload/documento, rotas/endpoints, migrations de constraints e testes citados.
- Módulos: `core`, `usuarios`, `cadastros`, `eventos`, `roteiros`, `oficios`, `justificativas`, `termos`, `planos_trabalho`, `ordens_servico`, `prestacoes_contas`, `documentos`, `protocolos`, `integracoes/google_drive`.
- Templates binários comprovados: `documentos/resources/oficio.docx`, `justificativa.docx`, três variantes de termo, dois planos, duas ordens, `relatorio-tecnico.docx` e `diario_bordo.xlsx`.
- JavaScript foi usado apenas como evidência complementar; nenhuma regra foi considerada segura somente por comportamento frontend sem backend/model/service correspondente.
