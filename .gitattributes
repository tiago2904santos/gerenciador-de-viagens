# Fim de linha canônico (NOVO-18)
#
# Arquivos de texto são versionados e materializados com LF em todas as
# plataformas. `text=auto` preserva arquivos binários, enquanto `eol=lf`
# impede que um editor no Windows reintroduza CRLF ou finais mistos.
* text=auto eol=lf
